# CLAUDE.md

**Start by reading [`_INDEX.md`](_INDEX.md)** — it is the project analysis index and
contains everything needed for context: what Sentinel is, the architecture, a file map,
the pipeline roles, agent tools, state model, payload schemas, configuration, how to
run/test, and the design invariants that changes must not break. Reading it replaces a
full "analyse the project" pass.

When you make changes that alter the architecture, file layout, tools, schemas,
configuration, or invariants described there, update `_INDEX.md` in the same commit so it
stays accurate.
